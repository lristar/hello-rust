[package]
name = "hello_cargo"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
rand = "0.7.0"

# 设置编译时优化代码的等级，等级越高优化的时间越长
[profile.dev]
opt-level = 0

[profile.release]
opt-level = 3

